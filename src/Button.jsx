import React from 'react'

const Button = ({color}) => {
  return (
    <div className='buts'>
       <button style={{background: color}} className='button'>Know more</button>
    </div>
    
  )
}

export default Button
